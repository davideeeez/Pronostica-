# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test tooling used only during development (Playwright etc.) and its output —
# never meant to be committed; kept out even if a script is added back temporarily.
verify*.mjs
verify*.ts
playwright-report/
test-results/
playwright/.cache/
shots/
screenshots/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local

# Claude Code personal/local settings (not meant to be shared via the repo)
.claude/settings.local.json
